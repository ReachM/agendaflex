services:
  postgres:
    image: postgres:16-alpine
    container_name: marcaiflex-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: agendaflex
      POSTGRES_PASSWORD: agendaflex
      POSTGRES_DB: agendaflex
    ports:
      - "5433:5432"
    volumes:
      - marcaiflex-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agendaflex -d agendaflex"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  marcaiflex-postgres-data:
